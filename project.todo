# CssUI Revival - Project TODO

## Pre-existing Issues
- [ ] Fix nullable reference warnings (many throughout codebase)

## Architecture & Design
- [ ] IWindowingSystem (window creation, input events) - pending

## Phase 4: DOM System Investigation & Fixes
### 4.1 Unit Test Coverage for DOM Attributes ✅ COMPLETED
- [x] Investigate setAttribute/hasAttribute bug (hasAttribute returns false after setAttribute) - FIXED
- [x] Create DOM attribute unit tests (24 tests passing)
  - [x] Test setAttribute with empty string value
  - [x] Test setAttribute with non-empty string value
  - [x] Test hasAttribute after setAttribute
  - [x] Test getAttribute after setAttribute
  - [x] Test removeAttribute
  - [x] Test attribute enumeration (getAttributeNames)
  - [x] Test toggleAttribute
  - [x] Test hasAttributes
- [x] Investigate AttributeValue.From() behavior with empty strings - WORKS CORRECTLY
- [x] Verify Attr.Value assignment and retrieval - WORKS CORRECTLY

#### Bugs Fixed During Testing:
1. XMLCommon.Is_NameChar() was missing '-' and '.' characters per XML spec
2. OrderedDictionary.Remove() wasn't updating MapInverse (index mapping corruption)
3. OrderedDictionary.UpdateIndicesAfterRemoval() wasn't decrementing indices after removal
4. Element.toggleAttribute() was passing Document instead of Element as owner to Attr constructor
5. AttributeValue.Parse() wasn't handling custom/data-* attributes (returned null definition)

### 4.2 DOM Element Tests ✅ COMPLETED
- [x] Test element creation (createElement, createElementNS) - 14 tests passing
- [x] Test element hierarchy (appendChild, removeChild, insertBefore) - 34 tests passing
- [x] Test element queries (getElementById, getElementsByClassName, getElementsByTagName) - 21 tests passing
- [x] Test element matching (matches, closest, querySelector, querySelectorAll) - 22 tests passing

#### Bugs Fixed During Testing:
1. DOMCommon.createElementNS() was passing Prefix as Namespace parameter to Create_Element()
2. DOMImplementation.createHTMLDocument() was adding duplicate DOCTYPE (HTMLDocument already adds one)
3. TreeWalker.nextNode() wasn't advancing node to sibling after finding it
4. TreeWalker.filterNode() wasn't subtracting 1 from nodeType for bit mask calculation
5. DOMCommon.Is_Descendant() was using TreeWalker incorrectly (root can't traverse to ancestors)
6. Node.Dom_replace_node_within_parent() had validation checks in wrong scope (else instead of inside if parent is Document)

#### Bugs Fixed During Query/Matching Tests:
7. AttributeDefinition.Parse() for String type returned raw string instead of AtomicString (getElementById failed)
8. AttributeValue.Parse() had arguments in wrong order (Value/Data swapped)
9. AttributeValue.AsString() didn't handle AtomicString values (returned null)
10. AtomicString.GetHashCode() was returning hash of MemoryHandle instead of string content
11. Node.Dom_remove_node_from_parent() wasn't removing elements from Document's ID map
12. DOMCommon.Get_Elements_By_Qualified_Name() case-insensitive comparison was backwards
13. CssSelector.Match_Against_Tree() was adding LinkedListNode to another list (invalid operation)
14. Element.closest() was using TreeWalker incorrectly (skipped self, started from wrong node)
15. DOMCommon.Get_Ancestors() TreeWalker couldn't traverse ancestors (root was set to starting node)
16. ComplexSelector.Match() applied combinator from wrong selector (broke descendant/child matching)

### 4.3 DOM Document Tests ✅ COMPLETED
- [x] Test document creation (createDocument, createHTMLDocument) - 14 tests passing
- [x] Test document tree operations - 8 tests passing
- [x] Test document fragment operations - 15 tests passing

#### Bugs Fixed During Document Tests:
17. Node.Dom_insert_node_into_parent_before() collection modification (iterate over ToArray() copy)
18. Node.Dom_replace_all_within_node() collection modification (iterate over ToArray() copy)
19. Element.textContent setter was using parentNode instead of this
20. DocumentFragment.textContent setter was using parentNode instead of this
21. DocumentFragment.textContent getter was using firstChild() instead of nextNode()
22. CssSelector.Match_Against_Tree() was casting DocumentFragment to Element (filter to Element only)
23. XMLDocument constructor always added doctype (added withoutDoctype constructor for createDocument)

### 4.4 DOM Advanced Features Tests
- [x] NodeIterator tests - 37 tests passing
  - [x] Test createNodeIterator with various filters
  - [x] Test nextNode/previousNode traversal
  - [x] Test filter callback behavior
  - [x] Test referenceNode and pointerBeforeReferenceNode
  - [x] Added Document.createNodeIterator() and Document.createTreeWalker() methods
  - [x] Exposed referenceNode and pointerBeforeReferenceNode as public properties per DOM spec
- [ ] ShadowRoot tests
  - [ ] Test attachShadow (open/closed modes)
  - [ ] Test shadowRoot property access
  - [ ] Test slot assignment
  - [ ] Test event retargeting
- [ ] Events tests
  - [ ] Test addEventListener/removeEventListener
  - [ ] Test dispatchEvent
  - [ ] Test event bubbling and capturing
  - [ ] Test stopPropagation/stopImmediatePropagation
  - [ ] Test event.target vs event.currentTarget
  - [ ] Test custom events
- [ ] Range tests
  - [ ] Test createRange
  - [ ] Test setStart/setEnd operations
  - [ ] Test selectNode/selectNodeContents
  - [ ] Test extractContents/cloneContents/deleteContents
  - [ ] Test Range boundary points
- [ ] MutationObserver tests
  - [ ] Test observe with childList option
  - [ ] Test observe with attributes option
  - [ ] Test observe with characterData option
  - [ ] Test subtree observation
  - [ ] Test disconnect and takeRecords

## Phase 5: Advanced CSS Features
### 5.1 CSS calc() Formal Tree (future enhancement)
- [ ] Sum/Product/Negate/Invert nodes (per spec)
- [ ] Simplification algorithm
- [ ] Proper serialization

## Test Coverage — 1137 PASSING (after NodeIterator tests completed)

### CSS Systems Test Status
- [ ] CSS Properties system (CSS/Properties/)
  - [ ] Property parsing tests
  - [ ] Property inheritance tests
  - [ ] Computed value resolution tests
  - [ ] Shorthand property expansion tests
- [ ] CSS Box Model (CSS/Box Model/)
  - [ ] CssPrincipalBox tests
  - [ ] BoxModel calculation tests
  - [ ] Margin collapsing tests
  - [ ] Containing block tests
- [ ] CSS Parser (CSS/Parser/) — Has known bugs
  - [ ] Stylesheet parsing tests
  - [ ] Rule parsing tests
  - [ ] Declaration parsing tests
  - [ ] Error recovery tests
- [ ] CSS Stylesheet/Rules (CSS/Stylesheet/, CSS/Rules/)
  - [ ] @media rule tests
  - [ ] @import rule tests
  - [ ] @font-face rule tests
  - [ ] Cascade and specificity tests
- [ ] CSS Custom Properties (CSS/Functions/)
  - [ ] var() resolution tests
  - [ ] Custom property inheritance tests
  - [ ] Cycle detection tests
- [ ] Block Formatting Context (CSS/Formatting/)
  - [ ] BlockFormattingContext tests
  - [ ] Float layout tests
  - [ ] Clear property tests

## Documentation

### Getting Started Guide
- [ ] Create GETTING_STARTED.md
  - [ ] Minimal project setup (NuGet package, project references)
  - [ ] Basic initialization code
  - [ ] Creating a simple document
  - [ ] Applying styles
  - [ ] Rendering output
  - [ ] Engine provider configuration
  - [ ] Headless mode usage (for testing/server-side)
- [ ] Add code examples for common scenarios
  - [ ] Parse and apply CSS stylesheet
  - [ ] Query elements by selector
  - [ ] Computed style access
  - [ ] Layout calculation
  - [ ] Custom render engine integration
