# CssUI Revival - Project TODO

## Pre-existing Issues
- [ ] Fix nullable reference warnings (many throughout codebase)

## Architecture & Design
- [ ] IWindowingSystem (window creation, input events) - pending
- [ ] Implement proper microtask queue system
  - [ ] Create microtask queue that executes after synchronous code completes (like JS event loop)
  - [ ] Integrate with .NET Task system (e.g., SynchronizationContext or custom scheduler)
  - [ ] Update Window.QueueObserverMicroTask() to use new system
  - [ ] Ensure MutationObserver callbacks fire at correct timing per W3C spec

## Phase 5: Advanced CSS Features
### 5.1 CSS calc() Formal Tree (future enhancement)
- [ ] Sum/Product/Negate/Invert nodes (per spec)
- [ ] Simplification algorithm
- [ ] Proper serialization

## Test Coverage — Total: 2294 passing, 233 skipped, 19 failing = 2546 tests
### DOM Tests: 518 passing, 3 skipped = 521 total

## Phase 6: CSS Test Coverage

### 6.1 Simple Tokens (CSS/Tokens/Simple/) ✓
- [x] ColonToken, CommaToken, SemicolonToken tests
- [x] Bracket tokens (Square, Curly, Paren) tests
- [x] Whitespace and Delim token tests

### 6.2 CSSValue System (CSS/Value/Base/) ✓
- [x] Activate and implement CssValueTests.cs stubs (71 passing, 9 skipped)
- [x] From_CSS parsing tests
- [x] Type conversion tests
- [x] Known bugs documented:
  - Bug: AsDecimal uses Convert.ToInt64 instead of Convert.ToDouble
  - Bug: CssValue.Equals throws InvalidCastException for integer comparison
  - Bug: From(double, ECssUnit) doesn't distinguish RESOLUTION from DIMENSION
  - Bug: CssTokenizer throws InvalidCastException when parsing percentages
  - Bug: Parser fails to parse '1em' dimension

### 6.3 CssParser (CSS/Parser/Base/) ✓
- [x] Activate and implement CssParserTests.cs stubs (127 tests passing)
- [x] Declaration parsing tests
- [x] Value list parsing tests
- [x] Function parsing tests
- [x] At-rule parsing tests (charset, import, font-face, keyframes, supports, namespace, page, layer)
- [x] QualifiedRule parsing tests (selectors, blocks)
- [x] SimpleBlock parsing tests
- [x] CssFunction encoding tests
- [x] Error recovery tests

### 6.4 CssUnitResolver (CSS/Types/) ✓
- [x] Unit conversion tests (px, em, rem, %, etc.)
- [x] Relative unit resolution tests
- [x] Known bugs documented:
  - Bug: Physical unit constants (INCH_TO_PX, MM_TO_PX, PT_TO_PX, PC_TO_PX, Q_TO_PX) use integer division
  - Bug: Resolution unit scales (DPI, DPCM) use integer division
  - Bug: GRAD scale uses integer division (400/360 = 1 instead of ~1.11)
  - Bug: Viewport units (VW, VH, VMIN, VMAX) return 1.0 fallback when visualViewport is unavailable

### 6.5 TokenStream (CSS/Deserialization/)
- [ ] Token iteration behavior tests
- [ ] Lookahead/consume tests

### 6.6 Parser Complex Tokens (CSS/Parser/Complex Tokens/)
- [ ] CssComponent tests
- [ ] CssFunction tests
- [ ] CssDecleration tests
- [ ] CssSimpleBlock tests

## Phase 7: CSS Properties System

### 7.1 CSS Properties (CSS/Properties/)
- [ ] Property parsing tests
- [ ] Property inheritance tests
- [ ] Computed value resolution tests
- [ ] Shorthand property expansion tests

### 7.2 CSS Box Model (CSS/Box Model/)
- [ ] CssPrincipalBox tests
- [ ] BoxModel calculation tests
- [ ] Margin collapsing tests
- [ ] Containing block tests

### 7.3 CSS Parser Integration (CSS/Parser/)
- [ ] Stylesheet parsing tests
- [ ] Rule parsing tests
- [ ] Declaration parsing tests
- [ ] Error recovery tests

### 7.4 CSS Stylesheet/Rules (CSS/Stylesheet/, CSS/Rules/)
- [ ] @media rule tests
- [ ] @import rule tests
- [ ] @font-face rule tests
- [ ] Cascade and specificity tests

### 7.5 CSS Custom Properties (CSS/Functions/)
- [ ] var() resolution tests
- [ ] Custom property inheritance tests
- [ ] Cycle detection tests

### 7.6 Block Formatting Context (CSS/Formatting/)
- [ ] BlockFormattingContext tests
- [ ] Float layout tests
- [ ] Clear property tests

## Documentation

### Getting Started Guide
- [ ] Create GETTING_STARTED.md
  - [ ] Minimal project setup (NuGet package, project references)
  - [ ] Basic initialization code
  - [ ] Creating a simple document
  - [ ] Applying styles
  - [ ] Rendering output
  - [ ] Engine provider configuration
  - [ ] Headless mode usage (for testing/server-side)
- [ ] Add code examples for common scenarios
  - [ ] Parse and apply CSS stylesheet
  - [ ] Query elements by selector
  - [ ] Computed style access
  - [ ] Layout calculation
  - [ ] Custom render engine integration
