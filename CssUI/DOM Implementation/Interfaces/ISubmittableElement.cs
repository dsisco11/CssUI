using CssUI.DOM.Events;

namespace CssUI.DOM
{
    /// <summary>
    /// Denotes elements that can be used for constructing the entry list when a form element is submitted.
    /// </summary>
    public interface ISubmittableElement : IEventTarget
    {
    }
}
